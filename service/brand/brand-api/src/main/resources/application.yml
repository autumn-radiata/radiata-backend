spring:
  application:
    name: brand-service
  config:
    import: classpath:application-database.yml

server:
  port: 8484

eureka:
  client:
    service-url:
      defaultZone: http://yunjae.click:19090/eureka/
  instance:
    hostname: yunjae.click



